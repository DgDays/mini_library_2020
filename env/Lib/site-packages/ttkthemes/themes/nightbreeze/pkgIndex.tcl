# Package index for tile demo pixmap themes.

if {[file isdirectory [file join $dir nightbreeze]]} {
    package ifneeded ttk::theme::nightbreeze 0.6 \
        [list source [file join $dir nightbreeze.tcl]]
}
